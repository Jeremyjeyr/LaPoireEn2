<p id="notice"><%= notice %></p>
<h1>Nom du produit</h1>


<%= simple_form_for farm_add_product_path[@farm, product] do |f|%>
  <%= f.number_field :quantity, value: 1, min: 1, max: :avalaible_stock %>
   <%= f.submit "Ajouter au panier"%>
 <%end%>

 <%= link_to 'Edit', edit_farm_product_path(@product) %>
 <%= link_to 'Back', farm_products_path %>
