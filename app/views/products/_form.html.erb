<%= simple_form_for [@farm.id, product.id] do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
    <% end %>
      <%= f.input :name %>
      <%= f.input :image %>
      <%= f.input :price %>
      <%= f.select :unit , [['Kilogrammes','Kg'],['Grammes','Gr'],['Par unité',"L'unité"]]%>
      <%= f.input :real_stock %>
      <div class="actions">
        <%= f.submit %>
      </div>
<% end %>
